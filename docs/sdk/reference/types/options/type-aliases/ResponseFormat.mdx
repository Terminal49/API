---
title: "Type Alias: ResponseFormat"
description: "ResponseFormat type alias in the Terminal49 TypeScript SDK, controlling whether methods return raw JSON:API responses, mapped models, or both for each request."
---

# Type Alias: ResponseFormat

> **ResponseFormat** = `"raw"` \| `"mapped"` \| `"both"`

Controls how SDK methods return API responses.
