---
title: Update tracking request custom field
description: "Update an existing custom field value on a tracking request in the Terminal49 API, identified by the custom field definition's api_slug for that record."
og:title: Update Tracking Request Custom Field | Terminal49 API Documentation
og:description: Update the value of a custom field on a specific tracking request by api_slug. Use custom fields to attach internal metadata like intake tags or customer references.
openapi: patch /tracking_requests/{tracking_request_id}/custom_fields/{api_slug}
---

Updates a specific custom field on a tracking request by its `api_slug`.

## Path parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `tracking_request_id` | Yes | The ID of the tracking request |
| `api_slug` | Yes | The api_slug of the custom field definition |

## Request body

| Parameter | Required | Description |
|-----------|----------|-------------|
| `value` | Yes | The new value to set |

## Authorization

Requires `update` permission on the tracking request.

## Response

Returns `200 OK` with the updated custom field resource on success.
