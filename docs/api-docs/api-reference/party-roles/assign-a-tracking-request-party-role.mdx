---
title: Assign a tracking request party role
description: "Assign a party to a tracking request in the Terminal49 API as shipper, consignee, freight forwarder, or another role."
og:title: Assign a Tracking Request Party Role | Terminal49 API Documentation
og:description: Assign a party to a tracking request in the Terminal49 API as shipper, consignee, freight forwarder, or another role.
openapi: post /tracking_requests/{tracking_request_id}/party_roles
---

Attaches a party from your account to the tracking request in one role.

## Path parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `tracking_request_id` | Yes | The ID of the tracking request |

## Roles

`shipper`, `consignee`, `notify_party`, `customs_broker`, `customer`, `freight_forwarder`, `pickup_dray_carrier`

## Behavior

- Roles are additive. Posting a second party with the same role keeps both
- To replace a party, delete its role and create a new one
- Posting the same party and role twice returns `422`
- The party must belong to your account, otherwise the request returns `401`
- Roles added to a tracking request are copied to its shipment, including when the shipment is created later
