/**
 * Config file management.
 *
 * Reads/writes ~/.config/terminal49/config.json (XDG-compliant).
 * Supports: token, baseUrl, defaultFormat, defaultOutput, maxRetries, color.
 */

// TODO: Implement in Phase 1
export {};
